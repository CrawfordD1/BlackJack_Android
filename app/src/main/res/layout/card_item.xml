<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <ImageView
        android:id="@+id/cardsuit"
        android:layout_width="158dp"
        android:layout_height="82dp"
        android:src="@mipmap/ic_launcher"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintHorizontal_bias="0.0"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintVertical_bias="0.0"
        app:srcCompat="@drawable/clubs"/>

    <TextView
        android:id="@+id/card_number"
        android:layout_width="36dp"
        android:layout_height="43dp"
        android:elevation="24dp"
        android:shadowColor="#000000"
        android:shadowRadius="12"
        android:textAlignment="center"
        android:textColor="#ffffff"
        android:textSize="31sp"
        android:textStyle="bold"
        app:layout_constraintBottom_toBottomOf="@+id/cardsuit"
        app:layout_constraintLeft_toLeftOf="@+id/cardsuit"
        app:layout_constraintRight_toRightOf="@+id/cardsuit"
        app:layout_constraintTop_toTopOf="@+id/cardsuit"
        tools:text="8"/>

</android.support.constraint.ConstraintLayout>